// named export
export function logMalWas(text) {
  console.log(text);
}

// default export
export default function logMalWasAnderes(text) {
  console.log(text);
}

// named export
export const a = 1337;

const b = 1337;
// export default b;
// Zweizeiler bei const decalarations für default export
